import React from 'react';
import { SearchBar, UserCard } from 'components';

function Home({ test }) {
  return (
    <div>
      <SearchBar />
      <UserCard />
    </div>
  );
}

export default Home;
